# Project Skills

이 폴더는 프로젝트 전용 스킬을 넣는 곳입니다.

- 커스텀 스킬을 추가하려면 `SKILL.md` 파일을 만든 뒤, 동일 폴더 구조로 배치하세요.
- 프로젝트 공용 규칙은 `.claude/settings.json`의 `permissions`/`hooks`에서 관리하세요.
- 기본으로는 `../settings.json`이 우선 적용되며, 필요 시 스킬로 확장합니다.
