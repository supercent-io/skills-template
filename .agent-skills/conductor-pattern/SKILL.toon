N:conductor-pattern
D:[→kanbanview] Conductor 패턴은 vibe-kanban(kanbanview)에 통합. CLI 전용 사용: bash scripts/conductor.sh로 병렬 에이전트 실행, bash scripts/pipeline.sh로 전체 파이프라인. Kanban UI 포함 사용은 kanbanview 스킬 참조.
G:conductor git-worktree parallel-agents pipeline kanbanview vibe-kanban merged cli-mode
U[3]:
  CLI 전용 병렬 실행: bash scripts/conductor.sh <feature> <base>
  전체 파이프라인: bash scripts/pipeline.sh <feature> --stages check,conductor,pr
  Kanban UI 포함 시각적 관리: kanbanview 스킬 사용 (vibe-kanban)
S[3]{n,action,details}:
  1,CLI Pipeline,bash scripts/pipeline.sh my-feature --stages check,conductor,pr
  2,Direct Conductor,bash scripts/conductor.sh my-feature main claude,codex
  3,Kanban UI,npx vibe-kanban (kanbanview 스킬 참조)
R[2]:
  Kanban UI가 필요하면 kanbanview 사용, CLI만 필요하면 pipeline.sh 직접 사용
  planno로 계획 검토 후 실행 권장
E[1]{desc,in,out}:
  "파이프라인 실행","bash scripts/pipeline.sh user-auth --stages check,conductor,pr","사전점검→병렬 에이전트→PR 자동 생성"
