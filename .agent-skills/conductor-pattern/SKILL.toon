N:conductor-pattern
D:[conductor] 동일 스펙을 Claude/Codex/Gemini가 git worktree 격리 샌드박스에서 병렬 구현. 에이전트별 PR 생성 후 best-of-both cherry-pick 병합. scripts/conductor.sh + pipeline.sh + hooks 시스템 포함.
G:conductor git-worktree parallel-agents claude-code codex gemini tmux pr-comparison pipeline multi-agent worktree
U[6]:
  동일 스펙을 여러 AI가 병렬 구현하고 결과 비교
  리팩터링 리스크 분산 (에이전트 실패 시 대안 확보)
  역할 분담 병렬 작업 (Claude: 로직, Codex: 테스트, Gemini: 문서)
  자동 파이프라인: check→conductor→PR (pipeline.sh)
  에이전트별 PR 비교 후 best-of-both cherry-pick 병합
  tmux 세션으로 에이전트 실시간 모니터링
S[5]{n,action,details}:
  1,Check,bash scripts/pipeline-check.sh (git/tmux/gh/jq/에이전트 CLI 사전 점검)
  2,Run,bash scripts/conductor.sh <feature> main claude,codex (worktree 생성 + tmux 병렬 실행)
  3,Monitor,tmux attach-session -t conductor-<feature> (실시간 진행 확인)
  4,PR,bash scripts/conductor-pr.sh <feature> (에이전트별 PR 자동 생성)
  5,Merge,gh pr list → cherry-pick best → git checkout -b feat/<feature>-best
R[4]:
  각 에이전트에 명확한 역할 지정 (중복 방지)
  pipeline.sh --dry-run으로 파이프라인 사전 확인
  worktree 완료 후 반드시 정리 (git worktree remove)
  hooks로 pre/post 검증 추가 가능 (CONDUCTOR_SKIP_HOOKS=1로 우회)
E[2]{desc,in,out}:
  "병렬 실행","bash scripts/conductor.sh user-dashboard main","trees/feat-user-dashboard-claude + trees/feat-user-dashboard-codex 생성 및 tmux 세션 실행"
  "전체 파이프라인","bash scripts/pipeline.sh user-auth --stages check,conductor,pr","사전점검→병렬 에이전트→PR 자동 생성"
