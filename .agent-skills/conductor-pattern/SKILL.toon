N:conductor-pattern
D:git worktree로 Claude/Codex/Gemini 에이전트를 병렬 실행. 동일 스펙을 독립 브랜치에서 동시 구현 후 PR로 비교. pipeline.sh로 check→conductor→pr 단계 자동화.
G:conductor git-worktree parallel-agents claude-code codex multi-agent pr-comparison
U[5]:
  동일 기능을 여러 AI 에이전트 버전으로 동시 구현 후 PR 비교
  난이도 높은 리팩터링 리스크 분산 (에이전트 실패 시 백업)
  Claude(설계)+Codex(테스트) 역할 분담 병렬 작업
  git 충돌 없이 여러 에이전트가 동시에 같은 레포 작업
  best-of-both 전략으로 최적 구현 선택
S[5]{n,action,details}:
  1,PreCheck,bash scripts/pipeline-check.sh --agents=claude,codex
  2,Run,bash scripts/conductor.sh <feature> main claude,codex
  3,Pipeline,bash scripts/pipeline.sh <feature> --stages check,conductor,pr
  4,PR,bash scripts/conductor-pr.sh <feature> main
  5,Cleanup,bash scripts/conductor-cleanup.sh <feature>
R[4]:
  --dry-run으로 먼저 미리보기 후 실행
  --resume으로 실패 단계부터 재개 (처음부터 재실행 불필요)
  CONDUCTOR_SKIP_HOOKS=1로 훅 우회 가능
  feature name은 자동 정규화 (소문자+하이픈만)
E[3]{desc,in,out}:
  "병렬 실행","bash scripts/conductor.sh user-dashboard main claude,codex","tmux 세션 생성: trees/feat-user-dashboard-claude + trees/feat-user-dashboard-codex"
  "PR 생성","bash scripts/conductor-pr.sh user-dashboard main","feat/user-dashboard-claude + feat/user-dashboard-codex 각각 PR 생성"
  "전체 파이프라인","bash scripts/pipeline.sh user-dashboard --stages check,conductor,pr","사전점검→병렬실행→PR 순서 자동 실행"
